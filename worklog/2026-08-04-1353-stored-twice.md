# Worklog: stored twice

Date: 2026-08-04T13:53:41Z (generated filename)
Thread: reducing the 91% that is me

## Looking at my own footprint

2.3 days of headroom and I am 91% of the cause. Flipping
`disable_content_logging` is Shaan's — it is gateway-wide and removes his
ability to inspect any request body. But **what my requests cost** is worth
understanding before asking him to absorb the loss.

```
raw_request               1.45 MB
responses_input_history   1.47 MB
content_summary           0.28 MB
raw_response              0.02 MB
```

The top two are near-identical in size. Reading both column heads on the same
row:

```
raw_request               {"model":"claude-opus-5","messages":[{"role":"user","content...
responses_input_history   [{"role":"system","content":[{"type":"input_text","text":"x-...
```

**The same conversation, serialised twice** — once as the outgoing request, once
as a replayable history.

## Not my path, the gateway

I checked whether this was a quirk of how the shim sends anthropic traffic:

```
anthropic     1.45 / 1.47   (1,502 req)
CodexOpenAI   1.04 / 0.96   (220 req)
Minimax       0.07 / 0.06   (54 req)
```

Every provider, the same ~1:1 ratio. **Bifrost duplicates the request body for
all traffic.**

## What that changes

I had costed `disable_content_logging` against `raw_request` alone. It also
eliminates `responses_input_history`, which is the same size again.

**The flag is worth twice what I told Shaan** — 2.92 of the 2.92 MB written per
request, not 1.45.

That is a materially better case for a decision I had already put in front of
him, and it came from asking "what exactly is in the 2.92 MB" rather than
accepting the total.

| Measurement | Before | After |
| --- | ---: | ---: |
| known cost per request | 2.92 MB (total) | **broken out by column** |
| flag's estimated saving | 1.45 MB/req | **2.92 MB/req** |
| providers checked for the pattern | 1 | **4** |

Verify exit 0.

## Still not applied

Nothing flipped. It is an observability decision — Shaan loses the ability to
inspect request bodies, including for debugging the very traffic that is filling
the disk. The escalation now carries the doubled figure so the trade is priced
correctly.
