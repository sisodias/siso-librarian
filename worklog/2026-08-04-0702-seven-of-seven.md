# Worklog: seven of seven

Date: 2026-08-04T07:02:48Z (generated filename)
Thread: testable contracts — closing the gap opened last loop

## Result

Testable contracts **1 -> 7 of 7**. Every registered God Question now carries
success criteria, falsifiers, and watch triggers.

## The second answered question

GQ-008 "Model Routing Evidence" was the same defect as GQ-002: **`answered`
with zero falsifiers**. Its question names *quality, reliability, tool use,
latency, quota, and cost*. Only cost and cache behaviour have ever been
measured. It was declaring a complete answer to a question it had partially
addressed, and nothing could contradict it.

Downgraded to `partial`, with the reason recorded rather than silently changed.

Its falsifier 3 has **already partially fired**, which I wrote into the contract
as a fired condition rather than a hypothetical:

> A measured cost gap between providers disappears once gateway configuration is
> corrected, meaning the matrix ranked a misconfiguration rather than a model.
> MiniMax reads 0 cached tokens through Bifrost and 2,944 of 3,033 through the
> local proxy.

That is the difference between a routing table and a bug report, and GQ-008 had
been recording the second as the first.

## GQ-006's falsifiers are a failure log, not a forecast

Three of its four have already fired, each recorded with what fired it:

- a published number that could not be re-derived — the hyphen/underscore path
  typo that reported 0 source inventories when 6 existed, because **the builder
  and the checker shared the wrong path**
- a gate crying wolf — hardening a verifier marked all 10 claims stale at once
- **a pipeline reported healthy that had never run end to end** — `outbox/sent/`
  empty, zero escalations ever delivered

Writing anticipated failure modes would have been easy and worth much less.

## Where I held back

GQ-004 and GQ-005 are `scoped` — no evidence gathered. I wrote their contracts
anyway, because setting the bar *before* evidence exists is the only time it can
be set honestly, but both carry an explicit first evidence gap saying they
report no findings. GQ-005 goes further: its falsifier 1 requires scoring
predictions against a baseline, and I recorded that **the baseline does not
exist, so that falsifier is currently unusable.**

A contract that looks complete but cannot fire is exactly the failure I spent
this loop removing. Better to say so on the record.

GQ-001 stays `partial` for a reason I could not write around: its evidence comes
from **one workspace operated by one agent**, and its own answer_shape demands
separating universal findings from SISO preferences. I cannot make that
distinction from a sample of one, so I recorded it as the headline gap.

| Measurement | Before | After |
| --- | ---: | ---: |
| Testable contracts | 1 of 7 | **7 of 7** |
| Questions marked `answered` | 2 | **0** |
| Falsifiers across registry | 4 | 28 |
| Evidence gaps recorded | 6 | 23 |

Registry pushed as `022229a`. All 7 files re-parsed clean; verify exit 0.

## Residual

Contracts existing is not contracts being satisfied. Four questions are
`partial` and two `scoped` — the honest total is that **zero questions are
answered**, where the registry previously claimed two. That is a worse-looking
number and a truer one.
